server:
  servlet:
    context-path: /phaedra/metadata-service
  port: 3002

spring:
  application:
    name: phaedra-metadata-service
  jackson:
    serialization:
      indent_output: true

eureka:
  client:
    enabled: true

logging:
  level:
    eu.openanalytics.phaedra: DEBUG